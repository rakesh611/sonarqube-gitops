apiVersion: v1
kind: Service
metadata:
  name: sonarqube-postgresql
  namespace: sonarqube
spec:
  type: ClusterIP

  selector:
    app: sonarqube-postgresql

  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
